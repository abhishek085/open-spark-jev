# Phase 1: supervised multi-task menu heads.
model: models/Qwen3-1.7B
output_dir: checkpoints/sft-qwen3-1.7b
data:
  - data/synthetic/sim_train.jsonl        # osj simulate --n 3000 --out data/synthetic/sim_train.jsonl
  - data/raw/public_train.jsonl           # osj public --limit 2000 --out data/raw/public_train.jsonl
  # - data/synthetic/teacher_train.jsonl  # osj synth --distill ...
seed: 0
val_frac: 0.1
max_len: 2048
epochs: 2
batch_size: 8
grad_accum: 4          # effective batch 32
lr: 2.0e-5
weight_decay: 0.01
max_grad_norm: 1.0
lambda_brier: 0.5      # Brier regulariser weight on top of CE / soft-target cross-entropy
gradient_checkpointing: true
aux_head: false        # true = learned linear menu head (ablation; not servable by TRT-LLM)
lora: null             # e.g. {r: 32, alpha: 64, dropout: 0.05} for LoRA instead of full FT
log_every: 10
